echo "--- Nginx Logs (LXC 101) ---" && pct exec 101 -- journalctl -u nginx -n 50 --no-pager && echo "\n--- Nginx Gateway Config (LXC 101) ---" && pct exec 101 -- cat /etc/nginx/sites-enabled/gateway && echo "\n--- Traefik Logs (LXC 102) ---" && pct exec 102 -- journalctl -u traefik -n 50 --no-pager && echo "\n--- Traefik Static Config (LXC 102) ---" && pct exec 102 -- cat /etc/traefik/traefik.yml && echo "\n--- Traefik Dynamic Config (LXC 102) ---" && pct exec 102 -- cat /etc/traefik/dynamic/dynamic_conf.yml && echo "\n--- Docker Daemon Logs (VM 1001) ---" && qm guest exec 1001 -- journalctl -u docker -n 50 --no-pager